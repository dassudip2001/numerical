#include<stdio.h>
#include<conio.h>
#include<math.h>
void main()
{
  float y,x,y1,y2,y3,y4,xf;
  clrscr();
  printf("Enter the value of y=");
  scanf("%f",&y);
  printf("Enter the value of x=");
  scanf("%f",&x);
  printf("Enter the value of x which is to be find=");
  scanf("%f",&xf);
  /*printf("Enter the value of y1=");
  scanf("%f",&y1);
  printf("Enter the value of y2:");
  scanf("%f",&y2);
  printf("Enter the value of y3=");
  scanf("%f",&y3);
  printf("Enter the value of y4=");
  scanf("%f",&y4); */
  y1=(x*y)+1;
  y2=y+(x*y1)+0;
  y3=y1+y1+(x*y2);
  y4=(2*y2)+y2+(x*y3);
  y=y+(xf*y1)+((pow(xf,2)/2)*y2)+((pow(xf,3)/6)*y3)+((pow(xf,4)/24)*y4);
  printf("Result is=%f",y);
  getch();
  }
